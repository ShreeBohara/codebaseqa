// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v5.29.3
// source: indexer.proto

package gen

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Indexer_IndexRepo_FullMethodName = "/codebaseqa.indexer.v1.Indexer/IndexRepo"
	Indexer_Health_FullMethodName    = "/codebaseqa.indexer.v1.Indexer/Health"
)

// IndexerClient is the client API for Indexer service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Repository indexing: file walk plus tree-sitter parsing, extracted from the Python API.
//
// WHY A SEPARATE SERVICE, STATED HONESTLY
// Not for parse throughput. Measured on this repository, ThreadPoolExecutor over the
// Python parser gives 1.02x (py-tree-sitter never releases the GIL) and
// ProcessPoolExecutor gives 2.84x, while only ~41% of the parse phase is actually C -- so
// a perfect native rewrite ceilings around 2.4x, below what a half-day change to the
// existing Python already achieves.
//
// The real reason is a process and failure boundary. repos.py runs indexing by spinning a
// new event loop inside an anyio threadpool thread and blocking it for minutes, in the
// same process that serves chat. This moves that CPU-bound, GIL-bound, minutes-long stage
// behind an explicit contract.
//
// Server streaming is the point of using gRPC here rather than one big response: progress
// becomes part of the contract instead of shared mutable state, which is the same bug
// class that made the SSE progress bar show only 0% or 100%.
type IndexerClient interface {
	// Walk and parse a checkout, streaming progress and chunk batches as they are produced.
	IndexRepo(ctx context.Context, in *IndexRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[IndexEvent], error)
	// Cheap liveness probe. Returns the grammars this build actually links, which is not
	// guaranteed to match the Python side -- see ParserInfo.
	Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error)
}

type indexerClient struct {
	cc grpc.ClientConnInterface
}

func NewIndexerClient(cc grpc.ClientConnInterface) IndexerClient {
	return &indexerClient{cc}
}

func (c *indexerClient) IndexRepo(ctx context.Context, in *IndexRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[IndexEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &Indexer_ServiceDesc.Streams[0], Indexer_IndexRepo_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[IndexRequest, IndexEvent]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Indexer_IndexRepoClient = grpc.ServerStreamingClient[IndexEvent]

func (c *indexerClient) Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HealthResponse)
	err := c.cc.Invoke(ctx, Indexer_Health_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// IndexerServer is the server API for Indexer service.
// All implementations must embed UnimplementedIndexerServer
// for forward compatibility.
//
// Repository indexing: file walk plus tree-sitter parsing, extracted from the Python API.
//
// WHY A SEPARATE SERVICE, STATED HONESTLY
// Not for parse throughput. Measured on this repository, ThreadPoolExecutor over the
// Python parser gives 1.02x (py-tree-sitter never releases the GIL) and
// ProcessPoolExecutor gives 2.84x, while only ~41% of the parse phase is actually C -- so
// a perfect native rewrite ceilings around 2.4x, below what a half-day change to the
// existing Python already achieves.
//
// The real reason is a process and failure boundary. repos.py runs indexing by spinning a
// new event loop inside an anyio threadpool thread and blocking it for minutes, in the
// same process that serves chat. This moves that CPU-bound, GIL-bound, minutes-long stage
// behind an explicit contract.
//
// Server streaming is the point of using gRPC here rather than one big response: progress
// becomes part of the contract instead of shared mutable state, which is the same bug
// class that made the SSE progress bar show only 0% or 100%.
type IndexerServer interface {
	// Walk and parse a checkout, streaming progress and chunk batches as they are produced.
	IndexRepo(*IndexRequest, grpc.ServerStreamingServer[IndexEvent]) error
	// Cheap liveness probe. Returns the grammars this build actually links, which is not
	// guaranteed to match the Python side -- see ParserInfo.
	Health(context.Context, *HealthRequest) (*HealthResponse, error)
	mustEmbedUnimplementedIndexerServer()
}

// UnimplementedIndexerServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedIndexerServer struct{}

func (UnimplementedIndexerServer) IndexRepo(*IndexRequest, grpc.ServerStreamingServer[IndexEvent]) error {
	return status.Error(codes.Unimplemented, "method IndexRepo not implemented")
}
func (UnimplementedIndexerServer) Health(context.Context, *HealthRequest) (*HealthResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Health not implemented")
}
func (UnimplementedIndexerServer) mustEmbedUnimplementedIndexerServer() {}
func (UnimplementedIndexerServer) testEmbeddedByValue()                 {}

// UnsafeIndexerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to IndexerServer will
// result in compilation errors.
type UnsafeIndexerServer interface {
	mustEmbedUnimplementedIndexerServer()
}

func RegisterIndexerServer(s grpc.ServiceRegistrar, srv IndexerServer) {
	// If the following call panics, it indicates UnimplementedIndexerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Indexer_ServiceDesc, srv)
}

func _Indexer_IndexRepo_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(IndexRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(IndexerServer).IndexRepo(m, &grpc.GenericServerStream[IndexRequest, IndexEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Indexer_IndexRepoServer = grpc.ServerStreamingServer[IndexEvent]

func _Indexer_Health_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HealthRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IndexerServer).Health(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Indexer_Health_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IndexerServer).Health(ctx, req.(*HealthRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// Indexer_ServiceDesc is the grpc.ServiceDesc for Indexer service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Indexer_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "codebaseqa.indexer.v1.Indexer",
	HandlerType: (*IndexerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Health",
			Handler:    _Indexer_Health_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "IndexRepo",
			Handler:       _Indexer_IndexRepo_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "indexer.proto",
}
